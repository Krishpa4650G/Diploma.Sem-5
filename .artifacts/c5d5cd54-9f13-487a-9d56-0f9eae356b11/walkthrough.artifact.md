# Walkthrough - Moved lib directory to /Flutter

I have successfully moved the `lib/` directory into the `Flutter/` subfolder using Git. This ensures that the code history is preserved and the repository structure is organized as requested.

## Changes Made

### Version Control
- **Moved**: `lib/` directory to `Flutter/lib/` using `git mv`.
- **Staged**: All changes in `Flutter/lib/` are now staged and ready for commit.

## Verification Results

### Manual Verification
- Verified that `Flutter/lib/` contains all the lab files (lab2 through lab15) and `main.dart`.
- Ran `git status` to confirm that the files are correctly marked as renamed or modified in their new location.

> [!TIP]
> You can now commit these changes with a message like "Move lib to Flutter subfolder" and push them to GitHub.
